# SMS Syncer Server Configuration
# Copy this file to .env and update values for your environment
# Required variables must be set for the server to start

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================

# Server port (default: 8080)
SERVER_PORT=8080

# Server host (default: localhost)
SERVER_HOST=localhost

# Enable TLS/HTTPS mode (default: false)
TLS_ENABLED=false

# TLS certificate and private key paths (required when TLS_ENABLED=true)
TLS_CERT_FILE=certs/dev/server.crt
TLS_KEY_FILE=certs/dev/server.key

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Database connection string (default: file:sms.db?cache=shared&mode=rwc)
# For SQLite: file:path/to/database.db?cache=shared&mode=rwc
# For PostgreSQL: user=postgres password=postgres dbname=sms_syncer host=localhost port=5432 sslmode=disable
DATABASE_DSN=file:sms.db?cache=shared&mode=rwc

# ============================================================================
# AUTHENTICATION & SECURITY (REQUIRED)
# ============================================================================

# JWT secret for token generation and validation (REQUIRED)
# Must be a strong, random string in production
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token expiry duration (default: 1h)
# Format: Go duration string (e.g., 1h, 30m, 24h)
JWT_TOKEN_EXPIRY=1h

# TOTP encryption key for 2FA secrets (REQUIRED)
# Must be exactly 64 hex characters (32 bytes in hex format)
# Generate with: openssl rand -hex 32
# CRITICAL: Keep this secret and never commit to version control
TOTP_ENCRYPTION_KEY=12345678901234567890123456789012abcdefabcdefabcdefabcdefabcdefab

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# ============================================================================
# SEED DATA CONFIGURATION
# ============================================================================

# Default admin username (default: admin)
ADMIN_USERNAME=admin

# Default admin password for initial setup (optional)
# If not provided, defaults to 'admin123'
# IMPORTANT: Change this immediately after first login
ADMIN_PASSWORD=admin123

# ============================================================================
# NOTES
# ============================================================================

# Security Best Practices:
# 1. Never commit .env file to version control (use .env.example only)
# 2. Generate strong random values for JWT_SECRET and TOTP_ENCRYPTION_KEY
# 3. Use environment variables in production, not .env files
# 4. Restrict file permissions: chmod 600 .env
# 5. Rotate secrets regularly
# 6. Use a secrets management system (AWS Secrets Manager, HashiCorp Vault, etc.)

# Development:
# - Copy this file to .env and update values
# - Load .env with: export $(cat .env | xargs)

# Production:
# - Set environment variables directly in your deployment system
# - Use a secrets management service
# - Never use default values for secrets
